import SwiftUI
import UIKit

// Mirrors client/tailwind.config.js — keep these in sync if the web palette changes.
enum ThreadiiTheme {
    static let steelColor = Color(red: 0x3D / 255.0, green: 0x6A / 255.0, blue: 0x85 / 255.0)
    static let creamColor = Color(red: 0xF1 / 255.0, green: 0xEA / 255.0, blue: 0xD9 / 255.0)
    static let inkColor = Color(red: 0x17 / 255.0, green: 0x14 / 255.0, blue: 0x12 / 255.0)
    static let taupeColor = Color(red: 0x8A / 255.0, green: 0x6F / 255.0, blue: 0x52 / 255.0)

    static let steel = UIColor(red: 0x3D / 255.0, green: 0x6A / 255.0, blue: 0x85 / 255.0, alpha: 1)
}
